# دليل استخدام سيرفر Mohamy Smart

هذا الدليل يشرح كيفية التعامل اليومي مع سيرفر الإنتاج (Production Server) وكيفية عمل النشر التلقائي.

## 🔑 1. معلومات الدخول الأساسية

- **IP Address:** `91.108.121.110`
- **Username:** `root`
- **ملف الباسورد:** البيانات الحساسة والباسورد محفوظة في ملف `.server-credentials` (موجود محلياً على جهازك فقط).

**للدخول إلى السيرفر من جهازك (Mac):**
افتح الـ Terminal واكتب:
```bash
ssh root@91.108.121.110
# سيطلب منك الباسورد (موجودة في .server-credentials)
```

---

## 🚀 2. النشر التلقائي (Auto-Deploy) وكيف يعمل

لقد قمنا بإعداد نظام **CI/CD** باستخدام GitHub Actions لتبسيط عملية التحديث.

### كيف ترفع تحديثات جديدة للسيرفر؟
ببساطة، ليس عليك فعل أي شيء معقد! العملية كالتالي:
1. اكتب الكود الخاص بك محلياً.
2. استخدم الأمر المجهز في الـ Makefile:
   ```bash
   make push MSG="وصف التحديث هنا"
   ```
   *(هذا الأمر سيقوم بعمل git add, commit, pull --rebase, ثم push)*

### ماذا يحدث خلف الكواليس؟
بمجرد أن يصل الكود لفرع `main` على GitHub:
1. يكتشف **GitHub Actions** التحديث (بناءً على ملف `.github/workflows/deploy.yml`).
2. يتصل بالسيرفر الخاص بك تلقائياً باستخدام بيانات الدخول المخفية (Secrets).
3. يُشغل أمر التحديث التلقائي على السيرفر (`/opt/mohamy-smart/scripts/deploy.sh`).
4. السيرفر يسحب أحدث كود، يبني الـ Docker Images الجديدة، ويعيد تشغيل التطبيق بدون توقف ملحوظ.

---

## 🛠️ 3. أوامر الإدارة والمراقبة (تُنفذ داخل السيرفر)

إذا أردت يوماً الدخول للسيرفر للاطمئنان أو التحقق من شيء، إليك أهم الأوامر:

**(يجب أن تكون متصلاً بالسيرفر عبر ssh أولاً)**

### أ. التحقق من حالة المنصة
```bash
cd /opt/mohamy-smart
docker compose --env-file .env.docker.prod -f docker-compose.prod.yml ps
```
*هذا الأمر سيعرض لك ما إذا كانت جميع الخدمات (backend, lawyer, admin, landing) تعمل (Up).*

### ب. عرض السجلات الحية (Live Logs)
لمراقبة ما يحدث في النظام (طلبات، أخطاء، الخ):
```bash
cd /opt/mohamy-smart
# سجلات كل شيء:
docker compose --env-file .env.docker.prod -f docker-compose.prod.yml logs -f

# سجلات الـ Backend فقط:
docker compose --env-file .env.docker.prod -f docker-compose.prod.yml logs -f backend
```
*(للخروج من السجلات اضغط `Ctrl + C`)*

### ج. فحص "صحة" السيرفر (المراقبة التلقائية)
لمعرفة ما إذا كان سكربت المراقبة التلقائية (Phase 4) قد تدخل لإعادة تشغيل أي خدمة:
```bash
tail -f /var/log/mohamy-health.log
```

### د. النشر اليدوي (إذا احتجت لذلك)
إذا أردت لأي سبب سحب التحديثات يدوياً بدون انتظار GitHub:
```bash
/opt/mohamy-smart/scripts/deploy.sh
```

### هـ. مراقبة استهلاك السيرفر (الرام والمعالج)
```bash
# مراقبة سريعة لكل الـ containers
docker stats

# مراقبة السيرفر ككل
htop
```

---

## ⚠️ 4. ملاحظات هامة

1. **البيانات الحساسة:** ملف `.env.docker.prod` موجود على السيرفر في مسار `/opt/mohamy-smart/`. هذا الملف يحتوي على مفاتيح API وقاعدة البيانات، ولا يُمحى عند التحديث.
2. **قاعدة البيانات:** بياناتك في أمان تام حتى مع التحديثات المتكررة لأنها تُحفظ في Volumes معزولة.
3. **لا تعدل الكود مباشرة على السيرفر:** دائماً عدل الكود على جهازك (Mac) ثم ارفعه `make push`. التعديل على السيرفر مباشرة سيتعارض مع التحديثات التلقائية.
